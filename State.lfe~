(defmodule State
  (export
   (state 1)
   ))



(defun state(x)
  (receive
    ((tuple 'put f)
     (state (funcall f x)))
    ((tuple 'get pid)
     (! pid x)
     (state x))
    (other
     (state x))))


